/*import '/index.dart';

 snackBarWidget({
  required String title,
  required String msg,
  Color? backgroundColor,
  Widget? icon,
}) =>
    Get.snackbar(
      title,
      msg, icon: icon,
      messageText: TextWidget(msg, fontWeight: FontWeight.w500),
      backgroundColor:
          backgroundColor ?? AppColors.primaryDeep.withOpacity(0.4),
      // borderRadius: getProportionateScreenWidth(4),
    ); */
